<%- include ("../partials/header") %>
<div class="container">
    <div class="box u-mt-10">
      <div class="edit-book-form">
        <form action="/author/book/details/<%= article.id %>?_method=PUT" method="POST">
          <div class="field">
            <h5 class="subtitle is-3 is-spaced">Edit Article</h5>
              <div class="field">
                <div class="control">
                  <input type="text" name="article[articleTitle]" class="input" placeholder="Title" value="<%= article.articleTitle %>">
                </div> 
              </div>
              <div class="field">
                <div class="control">
                  <input type="text" name="article[publishingYear]" class="input" placeholder="Year of Publication" value="<%= article.publishingYear %>">
                </div>
              </div>
              <div class="field">
                <div class="control">
                  <input name="article[articleLink]" class="input" placeholder="Publishing Link" value="<%= article.articleLink %>">
                </div>
              </div>
              <div class="field">
                <div class="control">
                    <button id="" class="button is-primary is-outlined">Save</button>
                </div>
            </div>
      </form>
      <form action="/author/book/details/<%= article.id %>?_method=DELETE" method="POST">
        <div class="field">
          <div class="control">
              <button id="" class="button is-danger is-outlined">Delete book</button>
          </div>
      </div>
    </form>
      </div>
    </div>
</div>